# Use the backend API (set to false for local-only / mobile build)
REACT_APP_USE_API=false

# Backend API base URL (optional; default is http://localhost:8080/api)
# REACT_APP_API_URL=http://localhost:8080/api
