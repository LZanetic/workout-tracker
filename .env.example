# App base path (used for GitHub Pages and local dev; build uses homepage from package.json)
# Set to /workout-tracker so the app runs at http://localhost:3000/workout-tracker and matches GitHub Pages.
PUBLIC_URL=/workout-tracker

# Use the backend API (true = use Spring backend, false = localStorage only)
# On Android/iOS app, API is auto-disabled when URL is localhost to avoid "Failed to fetch" errors.
REACT_APP_USE_API=true

# Backend API base URL (optional; default is http://localhost:8080/api)
# REACT_APP_API_URL=http://localhost:8080/api
